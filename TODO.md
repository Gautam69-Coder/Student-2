# TODO
- [ ] Redesign `client/src/pages/user/Profile.jsx` to match Home/dashboard styling (Card/CardTitle + theme colors + grid sections).
- [ ] Keep existing profile fetch/update/validation logic intact.
- [ ] Verify view-mode and edit-mode render without runtime errors.
- [ ] Run `npm run dev` or build to ensure imports compile.

